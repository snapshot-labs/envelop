export const getSignature = (email: string, address: string): string => {
  return '';
};

export const verifySignature = (email: string, address: string, signature: string): boolean => {
  return true;
};
