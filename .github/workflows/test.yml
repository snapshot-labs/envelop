name: Test

on: [push]

jobs:
  test:
    strategy:
      matrix:
        target: ['22']
    uses: snapshot-labs/actions/.github/workflows/test.yml@main
    secrets: inherit
    with:
      postgres_database_name: envelop_test
      postgres_schema_path: ''
      redis: true
      target: ${{ matrix.target }}
